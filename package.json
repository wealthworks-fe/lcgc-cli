{
  "name": "lcgc-cli",
  "version": "0.2.0",
  "description": "A framework base on umi",
  "engines": {
    "node": ">=8.0.0"
  },
  "bin": {
    "lcgc": "cli.js"
  },
  "scripts": {
    "lint": "umi-lint --eslint *{.js,.jsx}",
    "precommit": "umi-lint --staged --eslint --prettier --fix",
    "prepublishOnly": "np --no-cleanup --yolo --no-publish --tag",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "umi"
  ],
  "author": "zhongxia",
  "license": "MIT",
  "devDependencies": {
    "np": "^5.2.1",
    "umi-lint": "^2.0.2"
  },
  "dependencies": {
    "chalk": "^3.0.0",
    "execa": "^4.0.0",
    "inquirer": "^7.0.1",
    "random-color": "^1.0.1",
    "shelljs": "^0.8.3",
    "umi": "^2.12.7",
    "umi-plugin-cdn": "^1.1.0",
    "umi-plugin-mpa-pug": "^0.3.0"
  }
}
