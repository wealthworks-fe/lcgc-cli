{
  "name": "lcgc-cli",
  "version": "0.1.2",
  "description": "A framework base on umi",
  "engines": {
    "node": ">=8.0.0"
  },
  "bin": {
    "lcgc": "cli.js"
  },
  "scripts": {
    "lint": "umi-lint --eslint plugins",
    "precommit": "umi-lint --staged --eslint --prettier --fix",
    "prepublishOnly": "np --no-cleanup --yolo --no-publish --tag",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "umi"
  ],
  "author": "zhongxia",
  "license": "MIT",
  "devDependencies": {
    "np": "^5.2.1",
    "umi-lint": "^2.0.2"
  }
}
